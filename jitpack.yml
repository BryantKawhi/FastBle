jdk:
  - openjdk18
before_install:
  - sdk install java 18.0.2
  - sdk use java 18.0.2
install:
  - echo "Running a custom install command"
  - ./gradlew clean install -xtest
env:
  MYVAR: "custom environment variable"
